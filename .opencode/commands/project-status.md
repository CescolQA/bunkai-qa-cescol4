---
description: Live snapshot of this project's configuration, compiled fresh every run.
argument-hint: [mcp|env|jira|db|git]
---

Invoke skill `project-status` in mode `status`.
Forward `$ARGUMENTS` unchanged.
